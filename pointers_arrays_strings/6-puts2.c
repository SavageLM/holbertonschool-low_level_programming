#include <stdio.h>
#include "main.h"

/**
 * puts2 - entry to function
 * @str: string to be printed
 */

void puts2(char *str)
{

	int c = 0, i = 0;

	whule str[i++]
	{
		c++;
	}

	for (i = 0; i < c; i += 2)
	{
		_putchar(str[i]);
	}

	_putchar('\n');
}
