#include <stdio.h>
#include "main.h"

/**
 * mul - entry point to function
 * Return: returns the product of a and b
 * @a: input to check
 * @b: input to check
 */

int mul(int a, int b)
{
	return (a * b);
}
