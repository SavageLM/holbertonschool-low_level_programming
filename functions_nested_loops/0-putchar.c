#include <stdio.h>
#include <main.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	{
		char i = _;

		_putchar(i);

		i = p;

		_putchar(i);

		i = u;

		_putchar(i);

		i = t;

		_putchar(i);

		i = c;
		_putchar(i);

		i = h;
		_putchar(i);

		i = a;
		_putchar(i);

		i = r;

		_putchar(i);
	}

	return (0);
}
