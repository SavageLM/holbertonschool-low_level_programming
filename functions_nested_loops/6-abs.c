#include <stdio.h>
#include "main.h"

/**
 * _abs - Entry point
 * Return: Return 0 if successful
 * @n: input to check
 */

int _abs(int n);
{

	if (n > 0)

		return (n);

	else if (n < 0)

		return (-n);

	return (0);

}
