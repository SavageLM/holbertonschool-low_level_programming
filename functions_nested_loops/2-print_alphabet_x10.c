#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - Entry point
 *
 */

void print_alphabet_x10(void)
{
	char alpha;
	int x;

	while (x = 1; x < 11;)
		for (alpha = 'a'; alpha <= 'z'; alpha++)
		{
			_putchar(alpha);
		}
		{
			_putchar('\n');
			x++;
		}

}
