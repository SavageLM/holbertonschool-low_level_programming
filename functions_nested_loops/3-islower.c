#include <stdio.h>
#include "main.h"

/**
 * _islower - Entry point
 * Return: Return 0 if successful
 * @c: input to check
 */

int _islower(int c)
{
	int c;

	if ((c > 96) && (c < 123))
	{
		_putchar(49);
	}
	else
	{
		-putchar(48);
	}

	Return(0)

}
